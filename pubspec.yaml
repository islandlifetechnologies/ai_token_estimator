name: ai_token_estimator
description: AI Token Usage estimator as a runnable CLI
repository: https://github.com/islandlifetechnologies/ai_token_estimator
version: 1.0.0

environment:
  sdk: ^3.13.0

executables: 
  ai_token_estimator: ai_token_estimator

dependencies:
  args: ^2.7.0
  bpe: ^1.0.3
  embed_annotation: ^1.2.3
  intl: ^0.20.3
  glob: ^2.2.0
  logging: ^1.3.0
  path: ^1.9.1

dev_dependencies: 
  build_runner: ^2.16.0 
  embed: ^1.6.6
  ilt_lints: ^1.0.1

